# Aprovação

O débito é síncrono dentro do {lang:en-US}approve{/lang}

Chame `Payment.approve()` antes de `retry_failed(payment_id)`.

Arquivo: `/home/user/config.yaml`

Visite https://github.com/user/project para mais detalhes.

Edite /home/user/project/main.py antes de continuar.

```python
print("teste")
```

Texto com emoji 🚀 e **negrito** no final.
